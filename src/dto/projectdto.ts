/**
 * Classe DTO di Project. DEVE essere uguale (stesso nome classe, stessi attributi e stessi nomi) a
 * quello nel backend. 
 * 
 * 
 * @author Crapara team
 */
export class ProjectDTO {

   projectid: number;

   userid: number;

   projectname: string;
}